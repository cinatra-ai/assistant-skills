{
  "name": "@cinatra-ai/assistant-skills",
  "version": "0.1.2",
  "files": [
    "skills"
  ],
  "license": "Apache-2.0",
  "description": "Cinatra chat assistant skills — the chat's own system prompt plus its conversational sub-skills (company research, campaign creation, trigger creation). Registered into the skills catalog at workspace level so every member can use them.",
  "type": "module",
  "scripts": {
    "test": "node --test extension-kind-gate.test.mjs"
  },
  "cinatra": {
    "apiVersion": "cinatra.ai/v1",
    "kind": "skill",
    "dependencies": []
  }
}
